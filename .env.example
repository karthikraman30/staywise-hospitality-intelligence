# Local PostgreSQL (used by `make pg-load`, `make pg-quality`, `make pg-validate`).
# psql reads these standard libpq variables; copy to .env and `source .env` or export them.
PGDATABASE=staywise
PGHOST=localhost
PGPORT=5432
# PGUSER=your_user
# PGPASSWORD=your_password
